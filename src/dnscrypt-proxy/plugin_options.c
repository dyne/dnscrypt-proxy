
#include <config.h>
#include <assert.h>
#include <stdlib.h>

#include "plugin_options.h"

int
plugin_option_parse_str(DCPluginSupport * const dpcs, char *str)
{
    assert(dpcs != NULL);
    assert(str != NULL);

    return 0;
}
